import common from "../common.mjs";

const { <%= app %> } = common.props;

export default {
  ...common,
  key: "<%= key %>",
  name: "<%= name %>",
  description: "<%= description %>",
  type: "action",
  version: "<%= version %>",
  async run({ $ }) {},
};
