import <%= app %> from "../<%= app %>.app.mjs";

export default {
  props: {
    <%= app %>,
    commonProperty: {
      propDefinition: [
        <%= app %>,
        "commonProperty",
      ],
    },
  },
};
